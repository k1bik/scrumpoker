<%= button_to "Show", "/show_estimates", method: :post, params: {room_id: room.id}, disabled: !room.is_estimates_hidden %>
<%= button_to "Hide", "/hide_estimates", method: :post, params: {room_id: room.id}, disabled: room.is_estimates_hidden %>
<%= button_to "Clear", "/clear_all", method: :post, params: {room_id: room.id} %>